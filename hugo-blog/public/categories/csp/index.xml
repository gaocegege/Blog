<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Csp on 高策</title>
    <link>https://gaocegege.com/Blog/categories/csp/</link>
    <description>Recent content in Csp on 高策</description>
    <generator>Hugo</generator>
    <language>zh-CN</language>
    <managingEditor>gaocegege@hotmail.com (高策)</managingEditor>
    <webMaster>gaocegege@hotmail.com (高策)</webMaster>
    <lastBuildDate>Sun, 23 Oct 2016 00:00:00 +0000</lastBuildDate>
    <atom:link href="https://gaocegege.com/Blog/categories/csp/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>CSP课堂笔记之Hypervisor</title>
      <link>https://gaocegege.com/Blog/csp%E8%AF%BE%E5%A0%82%E7%AC%94%E8%AE%B0%E4%B9%8Bhypervisor/</link>
      <pubDate>Sun, 23 Oct 2016 00:00:00 +0000</pubDate><author>gaocegege@hotmail.com (高策)</author>
      <guid>https://gaocegege.com/Blog/csp%E8%AF%BE%E5%A0%82%E7%AC%94%E8%AE%B0%E4%B9%8Bhypervisor/</guid>
      <description>&lt;h2 id=&#34;background&#34;&gt;Background&lt;/h2&gt;&#xA;&lt;p&gt;CSP是上海交通大学软件学院的研究生课程，因为之前大四上的时候没好好上，特意重修了一遍，这里是重修时候课上的一些记录。&lt;/p&gt;&#xA;&lt;h2 id=&#34;正文&#34;&gt;正文&lt;/h2&gt;&#xA;&lt;p&gt;虚拟化是一个非常有趣的技术，对于不同的人，听到这个词可能都会有不同的想法。因为虚拟化的概念实在是太常见了。在这里我们只讨论对操作系统的虚拟化，通俗来讲就是如何在一台机器上跑多个系统的那种虚拟化。这样的虚拟化在 &lt;a href=&#34;https://en.wikipedia.org/wiki/Virtualization&#34;&gt;wiki&lt;/a&gt; 上被称作 &lt;a href=&#34;https://en.wikipedia.org/wiki/Hardware_virtualization&#34;&gt;Hardware Virtulization&lt;/a&gt;，或者是叫做 Platform Virtualization。&lt;/p&gt;&#xA;&lt;p&gt;这是一篇半科普的文章，并不是很严谨，有用词不慎还请指正。&lt;/p&gt;&#xA;&lt;h3 id=&#34;虚拟化的分类&#34;&gt;虚拟化的分类&lt;/h3&gt;&#xA;&lt;p&gt;就算是把重点放在 Hardware Virtulization 上，也存在好多种实现的方式，这些方式都是从不同角度来进行虚拟化的。如果想要了解这些方法，首先要介绍下一个操作系统的体系结构。&lt;/p&gt;&#xA;&lt;figure&gt;&#xA;&#x9;&lt;img src=&#34;http://docplayer.net/docs-images/26/9385319/images/70-0.png&#34; alt=&#34;Computer System Architecture&#34; height=&#34;400&#34; width=&#34;400&#34;&gt;&#xA;&#x9;&lt;figcaption&gt;Computer System Architecture&lt;/figcaption&gt;&#xA;&lt;/figure&gt;&#xA;&lt;p&gt;上面的图比较形象地介绍了从底层硬件到操作系统的整体架构。操作系统和硬件之间的那一层叫做 Instruction Set Architecture (ISA)。这里就是指底层的那些指令，比如 load, store, call, return 等。其中有一些，是特权指令，只有运行在 ring 0 模式下的应用可以使用这些指令，也就是内核。这样的指令包括 hlt, load cr3 等。这样的指令，就是图中 3 所代表的 System ISA。而用户可以使用的指令，比如 call, return 这些，就是 4 所代表的 User ISA。两者共同组成了 ISA。&lt;/p&gt;&#xA;&lt;p&gt;再上一层，是 ABI，也就是操作系统和库之间的那一层。它包括系统调用，和之前提到的 User ISA，也包括一些调用协定，比如调用方和被调用方各自保存哪些寄存器的值这样的。这一层对编译器的实现有很大的影响。&lt;/p&gt;&#xA;&lt;p&gt;再往上一层就是广为人知的 API，作为一个只会使用 API 的程序员，了解下面的东西还真花了一些功夫，但 API 的概念想必早就已经深入人心了，这里就不再去介绍了。&lt;/p&gt;&#xA;&lt;figure&gt;&#xA;&#x9;&lt;img src=&#34;http://docplayer.net/docs-images/24/3269448/images/11-0.png&#34; alt=&#34;Process and System VM&#34; height=&#34;400&#34; width=&#34;400&#34;&gt;&#xA;&#x9;&lt;figcaption&gt;Process and System VM&lt;/figcaption&gt;&#xA;&lt;/figure&gt;&#xA;&lt;p&gt;那如果要实现虚拟化，可以从 ABI 和 API 两个层次来实现，这两种实现都要跟 ISA 打交道。上图说明了两种实现的结构，Process VM 可以实现在 Host OS 上运行其他 ISA 或 OS specific 的程序，这可以被当做是在 API 层次上实现了虚拟化。通常来说，Process VM 会有一个图中所示的 Runtime，Runtime 跟 Guest 代码是在一个进程里的，所有 Guest 代码的执行都会在其中。&lt;/p&gt;</description>
    </item>
    <item>
      <title>CSP课堂笔记之UniKernel</title>
      <link>https://gaocegege.com/Blog/csp%E8%AF%BE%E5%A0%82%E7%AC%94%E8%AE%B0%E4%B9%8Bunikernel/</link>
      <pubDate>Wed, 19 Oct 2016 00:00:00 +0000</pubDate><author>gaocegege@hotmail.com (高策)</author>
      <guid>https://gaocegege.com/Blog/csp%E8%AF%BE%E5%A0%82%E7%AC%94%E8%AE%B0%E4%B9%8Bunikernel/</guid>
      <description>&lt;h2 id=&#34;background&#34;&gt;Background&lt;/h2&gt;&#xA;&lt;p&gt;CSP是上海交通大学软件学院的研究生课程，因为之前大四上的时候没好好上，特意重修了一遍，这里是重修时候课上的一些记录。&lt;/p&gt;&#xA;&lt;h2 id=&#34;正文&#34;&gt;正文&lt;/h2&gt;&#xA;&lt;h3 id=&#34;monolithic-kernel&#34;&gt;Monolithic Kernel&lt;/h3&gt;&#xA;&lt;p&gt;相信只要是接触过计算机的，一定对『内核』这两个字不会陌生。只要在使用电脑的时候，都离不开跟内核打交道。无论是Windows还是Linux等等，这些寻常使用的操作系统，都是用内核和管理和使用硬件的。&lt;/p&gt;&#xA;&lt;p&gt;在这其中，最有名的是Linux Kernel，最早是由Linus实现的。看到这个标题还会点开的，应该都是听说过Linux Kernel的吧。它是一个典型的Monolithic Kernel，也就是我们说的宏内核。&lt;/p&gt;&#xA;&lt;p&gt;Monolithic，词典义是『单一的，庞大的』，简单理解，就是内核的所有功能，从文件系统，到内存管理，到CPU调度，等等等等，都放在内核态中。这样做有一个很大的好处，那就是所有这些功能都在同一个地址空间下，大家做通信会非常方便，而且通信的成本肯定也是低的，实现起来会快糙猛一些。就是很莽很厉害。&lt;/p&gt;&#xA;&lt;p&gt;但是，这样也会导致一些其他问题。我觉得最大的问题就是代码复杂度会提高。Linux Kernel的代码量太美不看。同时，这样也会导致容错性不是很好，只要一个地方出了问题，会导致整个Kernel都挂掉。&lt;/p&gt;&#xA;&lt;h3 id=&#34;micro-kernel&#34;&gt;Micro Kernel&lt;/h3&gt;&#xA;&lt;p&gt;那既然Monolithic Kernel有这样的问题，做研究的人一定不会放过这样的好机会，于是就有了微内核的概念。所谓微内核，是指内核只提供最必要的功能，比如IPC，内存管理，CPU调度等等。而其他，诸如文件系统，网络IO等等，都放在用户态来实现。&lt;/p&gt;&#xA;&lt;p&gt;这样做的好处首先是内核变小了，内核是常驻内存的，小了自然就节约了内存的空间。但是，这样的性能就会不如很莽很厉害的Monolithic Kernel要好，因为IPC通信的overhead还是有一些的。&lt;/p&gt;&#xA;&lt;h3 id=&#34;hybrid-kernel&#34;&gt;Hybrid Kernel&lt;/h3&gt;&#xA;&lt;p&gt;那既然Micro Kernel和Monolithic Kernel都有问题，能不能把两个结合一下，互相中和呢。&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;PPAP (Kernel Version)&#xA;I have a Monolithic Kernel.&#xA;I have a Micro Kernel.&#xA;Bow!&#xA;Hybrid Kernel!&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;这大概是研究的惯用套路，一个事情有两种极端的做法，然后中和一下，就有了第三种做法，公有云，私有云，混合云也是这样的套路，呃扯远了。那Hybrid Kernel就是把一部分不常使用的内核模块，或者是原本需要的时间就很长，因此IPC的overhead看起来就不是那么夸张的功能，移出内核，而其他的就会放在内核里。&lt;/p&gt;&#xA;&lt;figure&gt;&#xA;&#x9;&lt;img src=&#34;https://upload.wikimedia.org/wikipedia/commons/thumb/d/d0/OS-structure2.svg/800px-OS-structure2.svg.png&#34; alt=&#34;Hybrid Kernel&#34; height=&#34;800&#34; width=&#34;800&#34;&gt;&#xA;&#x9;&lt;figcaption&gt;Hybrid Kernel&lt;/figcaption&gt;&#xA;&lt;/figure&gt;&#xA;&lt;p&gt;Windows就是这样的内核，当然，这个是听别人说的，不是很懂他们是怎么看得到Windows的内核的，应该是论文吧，嗯。&lt;/p&gt;&#xA;&lt;h3 id=&#34;exokernel&#34;&gt;ExoKernel&lt;/h3&gt;&#xA;&lt;p&gt;看上去，内核已经被研究人员们研究地没得玩了。但ExoKernel又是另外一种内核，跟前面的几种都有不同。而且ExoKernel的风格，感觉上跟Xen，kvm这样的VMM有很大程度的相似，包括某些部分的实现，比如内存管理上。&lt;/p&gt;&#xA;&lt;p&gt;之前说到的Monolithic Kernel也好，Micro Kernel也好，它们对资源的保护和管理都是在内核里去做的，也就是说，它们都是先对硬件做抽象，然后向上不直接暴露硬件，而是暴露硬件的抽象。一个例子，硬盘是硬件设备，但是我们的应用程序看到的是一个个的文件，文件本身就是一种抽象。而应用程序，是没办法直接操作硬件的，只能跟操作系统提供的抽象打交道。&lt;/p&gt;&#xA;&lt;p&gt;那这样无疑是使得写应用的程序员不需要关心硬件，但是对于一些应用而言，其实是不好的。有一部分应用，就是需要跟硬件直接打交道。课上夏老师举的例子，是说数据库，MySQL，特别希望自己能够直接跟磁盘来交互，这样可以精确地确定它的索引等等文件的存储位置，来针对硬件来做优化。&lt;/p&gt;&#xA;&lt;p&gt;那这样的需求，之前的内核都是没办法满足的。于是，ExoKernel就被提了出来。Exokernel的设计思路是尽可能减少抽象层次，允许应用程序直接访问硬件，而ExoKernel只负责保护和分配系统资源。应用程序过来请求资源，Exokernel看看资源是否空闲，如果空闲，直接交给应用，至于应用怎么访问是它自己的事。&lt;/p&gt;&#xA;&lt;p&gt;ExoKernel分离了对硬件的使用和保护，使得应用程序可以直接使用没有经过Kernel抽象的硬件，就很好很强大。但是这样又有了其他的问题，那如果一个应用想发个HTTP请求，总不能先自己实现一套网络栈吧。于是，LibOS的概念就出现了&lt;/p&gt;&#xA;&lt;p&gt;至于实现，其实有很多地方跟Xen很像，比如说都会用Software-defined TLB来做内存控制，就是在Flush TLB后，触发TLB Miss就会trap到ExoKernel，这个时候会先检查合法性，然后再填适当的缓存项到TLB中。要是没有Software-defined TLB就用页表来做。LibOS只能读，不能写自己的页表，所有对页表的写，都会trap到ExoKernel，然后ExoKernel会在这次trap中做检查，跟Software-defined TLB差不多，只是在不同层次的实现，但实现方式都是trap的方式。等等，还有很多地方。&lt;/p&gt;&#xA;&lt;h4 id=&#34;libos&#34;&gt;LibOS&lt;/h4&gt;&#xA;&lt;p&gt;所谓LibOS，全称是Library Operation System。LibOS提供对于硬件的抽象，与用户代码编译成一个二进制，在同一地址空间。而且LibOS可以修改定制，来适配上层用户对硬件的具体需求，如控制物理内存的相邻等等。&lt;/p&gt;&#xA;&lt;figure&gt;&#xA;&#x9;&lt;img src=&#34;https://gaocegege.com/Blog/images/unikernel/exokernel.png&#34; alt=&#34;ExoKernel&#34; height=&#34;500&#34; width=&#34;500&#34;&gt;&#xA;&#x9;&lt;figcaption&gt;ExoKernel&lt;/figcaption&gt;&#xA;&lt;/figure&gt;&#xA;&lt;p&gt;这样，不仅简化了内核的设计，也使得应用可以针对硬件做优化。但是，缺点是内核里没有进程调度，所以没有真正的多进程，而是靠Kernel来进行资源的分配。&lt;/p&gt;&#xA;&lt;h3 id=&#34;unikernel&#34;&gt;UniKernel&lt;/h3&gt;&#xA;&lt;p&gt;Unikernel是一种LibOS，它有着自己的哲学，那就是一个操作系统应该只有一个进程，内核和应用都在一个地址空间内。这样的好处就是，可以使得这个操作系统，或者说应用，非常地快。因为操作系统所有的资源都是这个应用的，而且所有对象都在一个地址空间里。还安全，相当于是可以剪裁原本的Monolithic Kernel，抛弃掉不用的功能，只把需要的硬件抽象Library编译到二进制里面。&lt;/p&gt;&#xA;&lt;p&gt;有这么多好处，不是它火的关键，最关键的是云计算的流行。Unikernel可以直接运行在Xen上，而且不像传统OS那样，它小，而且安全，还快，简直就是天生为云计算而生的。从某种程度而言，是云计算中理想的Guest OS。&lt;/p&gt;</description>
    </item>
  </channel>
</rss>
